name: jax-fenics-env
channels:
  - conda-forge
  - nvidia  # needed for cuda-toolkit. if specific version is required, this needs to be changed.
dependencies:
  - numpy
  - matplotlib
  - fenics
  - connected-components-3d
  - scipy
  - pip
  - h5py
  - scikit-image
  - nibabel
  - svmtk <= 1.3 # Not sure if this is still needed, but I have previously had issues with newer version of svmtk-conda,
  - meshio
  - tqdm
  - parse
  - optax
  - cuda-toolkit 
  - pip: 
    - jax
    - jaxtyping
    - jaxopt
    - typeguard
    - git+https://github.com/dolfin-adjoint/pyadjoint.git@2019.1.2
